export interface SeedsResponse {
    royale: {
        production: string
        staging: string
        dev: string
    }
    trials: {
        production: string
        staging: string
        dev: string
    }
}
